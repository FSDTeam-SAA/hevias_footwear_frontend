export interface BlogPost {
 id: string;           // 👈 change number → string
  title: string;
  description: string;
  date: string;
  image: string;
  _id: string;
  createdBy: string;
  createdAt: string;
  updatedAt: string;
}
